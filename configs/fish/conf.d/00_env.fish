set -xg fish_greeting

set -xg fish_emoji_width 1
set -xg fish_ambiguous_width 1

set -xg LANG en_US.UTF-8
set -xg LC_ALL en_US.UTF-8

set -xg PATH /opt/homebrew/ $PATH
set -xg PATH /opt/homebrew/bin/ $PATH
set -xg PATH /sbin/ $PATH
set -xg PATH /usr/sbin/ $PATH
set -xg PATH /bin/ $PATH
set -xg PATH /usr/bin/ $PATH
set -xg PATH /usr/local/bin/ $PATH
set -xg PATH /usr/local/sbin/ $PATH
set -xg PATH $HOME/.cargo/bin $PATH
